# TODO: Make functionlity for this interaction
def new_request(say):
    say('New request')
    
# Pulls one request from DB on invocation and sends it to user, along with the option to respond or see a new one
def view_request(say):
    say('view request')